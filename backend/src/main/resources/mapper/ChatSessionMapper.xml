<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.claw.agent.mapper.ChatSessionMapper">

    <!--
        插入或刷新会话：利用唯一索引 uk_user_session(username, session_id)，
        存在则更新 update_time（最后活跃时间），不存在则插入。
        单次 SQL 替代 SELECT + INSERT/UPDATE，避免每条消息都触发两次 DB 操作。
    -->
    <insert id="insertOrUpdate">
        INSERT INTO chat_session (tenant_id, session_id, username, title, create_time, update_time)
        VALUES (#{tenantId}, #{sessionId}, #{username}, #{title}, NOW(), NOW())
        ON DUPLICATE KEY UPDATE update_time = NOW()
    </insert>

</mapper>
