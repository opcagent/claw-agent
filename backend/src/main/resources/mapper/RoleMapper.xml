<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<!--
  角色表自定义 SQL 映射。
  BaseMapper 内置 CRUD 由 MyBatis Plus 自动生成，此处仅放联表等自定义查询。
-->
<mapper namespace="com.claw.agent.mapper.RoleMapper">

    <!-- 角色结果映射（sys_role 全字段） -->
    <resultMap id="roleResultMap" type="com.claw.agent.model.Role">
        <id column="id" property="id"/>
        <result column="tenant_id" property="tenantId"/>
        <result column="role_name" property="roleName"/>
        <result column="role_key" property="roleKey"/>
        <result column="role_sort" property="roleSort"/>
        <result column="data_scope" property="dataScope"/>
        <result column="status" property="status"/>
        <result column="remark" property="remark"/>
        <result column="create_time" property="createTime"/>
        <result column="update_time" property="updateTime"/>
    </resultMap>

    <!-- 查询用户的全部启用角色（经 sys_user_role 关联），供登录时写入 JWT -->
    <select id="selectRolesByUserId" resultMap="roleResultMap">
        SELECT r.id, r.tenant_id, r.role_name, r.role_key, r.role_sort,
               r.data_scope, r.status, r.remark, r.create_time, r.update_time
        FROM sys_role r
                 INNER JOIN sys_user_role ur ON ur.role_id = r.id
        WHERE ur.user_id = #{userId}
          AND r.status = 1
        ORDER BY r.role_sort
    </select>

</mapper>
